# パラメータ調整

セグメンテーションのパラメータを調整する。

ユーザーに以下を確認する：
1. 現在のセグメント数は多すぎるか、少なすぎるか
2. 検出したい模様の大きさ（細かい/粗い）

その回答に基づいて：
- セグメントが多すぎる → `threshold`を上げる（例: 0.02 → 0.05）
- セグメントが少なすぎる → `threshold`を下げる（例: 0.02 → 0.01）
- 小さなノイズが多い → `min_segment_size`を上げる

`pottery_segmentation.cpp`の該当行を編集し、変更内容と理由を説明する。
